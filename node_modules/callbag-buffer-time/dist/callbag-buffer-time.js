'use strict';

function _interopDefault (ex) { return (ex && (typeof ex === 'object') && 'default' in ex) ? ex['default'] : ex; }

var buffer = _interopDefault(require('callbag-buffer'));
var interval = _interopDefault(require('callbag-interval'));

function bufferTime(ms) {
  return function (source) {
    return buffer(interval(ms))(source);
  };
}

module.exports = bufferTime;
