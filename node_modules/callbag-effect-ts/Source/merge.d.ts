import { EffectSource } from "../Source.js";
export declare const mergeIdentical: <R, E, A>(...sources: EffectSource<R, E, A>[]) => EffectSource<R, E, A>;
export declare const merge: <R1, E1, B>(fb: EffectSource<R1, E1, B>) => <R, E, A>(self: EffectSource<R, E, A>) => EffectSource<R1 | R, E1 | E, B | A>;
