import * as T from "@effect/io/Effect";
import { EffectSource } from "../Source.js";
export declare const alt: <R1, E, E1, B>(f: (e: E) => T.Effect<R1, E1, B>) => <R, A>(self: EffectSource<R, E, A>) => EffectSource<R1 | R, E1, B | A>;
