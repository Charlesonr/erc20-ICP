import * as T from "@effect/io/Effect";
import { EffectSource } from "../Source.js";
import * as Scope from "@effect/io/Scope";
export declare const unwrapScope: <R, R1, E, E1, A>(mfa: T.Effect<R1, E1, EffectSource<R, E, A>>) => EffectSource<R | Exclude<R1, Scope.Scope>, E | E1, A>;
