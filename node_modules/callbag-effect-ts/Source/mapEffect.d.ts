import * as T from "@effect/io/Effect";
import { EffectSource } from "../Source.js";
export declare const mapEffect: <R1, E1, A, B>(fab: (a: A) => T.Effect<R1, E1, B>) => <R, E>(self: EffectSource<R, E, A>) => EffectSource<R1 | R, E1 | E, B>;
