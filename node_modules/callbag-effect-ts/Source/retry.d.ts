import * as SC from "@effect/io/Schedule";
import { EffectSource } from "../Source.js";
export declare const retry: <R1, E, Z>(schedule: SC.Schedule<R1, E, Z>) => <R, A>(self: EffectSource<R, E, A>) => EffectSource<R1 | R, E, A>;
