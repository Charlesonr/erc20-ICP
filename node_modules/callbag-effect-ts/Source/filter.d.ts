import { EffectSource } from "../Source.js";
export declare const filter: {
    <A, B extends A>(fab: (a: A) => a is B): <R, E>(self: EffectSource<R, E, A>) => EffectSource<R, E, B>;
    <A>(fab: (a: A) => boolean): <R, E>(self: EffectSource<R, E, A>) => EffectSource<R, E, A>;
};
