import * as T from "@effect/io/Effect";
import { EffectSource } from "../Source.js";
export declare const tap: <R1, E1, A>(f: (a: A) => T.Effect<R1, E1, any>) => <R, E>(self: EffectSource<R, E, A>) => EffectSource<R1 | R, E1 | E, A>;
