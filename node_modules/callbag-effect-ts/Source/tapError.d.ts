import * as T from "@effect/io/Effect";
import { EffectSource } from "../Source.js";
export declare const tapError: <R1, E, E1>(f: (e: E) => T.Effect<R1, E1, unknown>) => <R, A>(self: EffectSource<R, E, A>) => EffectSource<R1 | R, E | E1, A>;
