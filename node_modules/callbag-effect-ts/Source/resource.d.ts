import { EffectSource } from "../Source.js";
import * as O from "@fp-ts/data/Option";
export declare const resource: <R, R1, E, E1, A, Acc>(initial: Acc, project: (acc: Acc, index: number) => EffectSource<R1, E1, readonly [O.Option<Acc>, EffectSource<R, E, A>]>, cleanup?: ((acc: Acc) => void) | undefined) => EffectSource<R | R1, E | E1, A>;
