import * as CB from "strict-callbag-basics";
/**
 * @tsplus static callbag-effect-ts/Source.EffectSource.Ops overridePull
 */
export const overridePull = (self, initialPulls = 1) => {
    const pulls = new Set();
    const source = (r) => {
        const [s, pull] = CB.overridePull(self(r), initialPulls);
        pulls.add(pull);
        return s;
    };
    return [source, () => pulls.forEach((f) => f())];
};
//# sourceMappingURL=overridePull.js.map