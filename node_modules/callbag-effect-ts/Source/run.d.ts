import * as T from "@effect/io/Effect";
import { EffectSource } from "../Source.js";
import { EffectSink } from "../Sink.js";
export declare const run: <R, R1, EI, EO, A>(self: EffectSource<R, EI, A>, sink: EffectSink<R1, EI, EO, A>) => T.Effect<R | R1, EI | EO, void>;
export declare const runDrain: <R, E>(self: EffectSource<R, E, unknown>) => T.Effect<R, E, void>;
