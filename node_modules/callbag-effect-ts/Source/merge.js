import * as CB from "strict-callbag-basics";
export const mergeIdentical = (...sources) => (r) => CB.mergeIdentical(...sources.map((s) => s(r)));
export const merge = (fb) => (self) => (r) => CB.merge_(self(r), fb(r));
//# sourceMappingURL=merge.js.map