import * as T from "@effect/io/Effect";
import { EffectSource } from "../Source.js";
export declare const share: <R, E, A>(self: EffectSource<R, E, A>) => T.Effect<R, never, EffectSource<never, E, A>>;
