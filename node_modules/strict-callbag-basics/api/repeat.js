import { repeatWhile_ } from "./repeatWhile.js";
export const repeat_ = (self, count) => repeatWhile_(self, (index) => index < count);
export const repeat = (count) => (self) => repeat_(self, count);
