import { Source } from "strict-callbag";
export declare const map_: <A, E, B>(self: Source<A, E>, f: (a: A, index: number) => B) => Source<B, E>;
export declare const map: <A, B>(f: (a: A, index: number) => B) => <E>(source: Source<A, E>) => Source<B, E>;
