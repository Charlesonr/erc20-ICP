import { Source } from "strict-callbag";
export declare const delay_: <A, E>(self: Source<A, E>, delayMs: number) => Source<A, E>;
export declare const delay: (delayMs: number) => <A, E>(self: Source<A, E>) => Source<A, E>;
