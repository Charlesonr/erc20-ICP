import cbTakeWhile from "callbag-take-while";
export function takeWhile_(self, pred) {
    return cbTakeWhile(pred)(self);
}
export function takeWhile(pred) {
    return (self) => takeWhile_(self, pred);
}
