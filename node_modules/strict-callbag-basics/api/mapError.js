export const mapError_ = (self, f) => (_, sink) => {
    self(0, (t, d) => {
        if (t === 2 && d) {
            sink(t, f(d));
        }
        else {
            sink(t, d);
        }
    });
};
export const mapError = (f) => (self) => mapError_(self, f);
