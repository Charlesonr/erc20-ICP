import { Source } from "strict-callbag";
/**
 * Converts any type of stream into a pull based one.
 *
 * `bufferSize` configures the amount of items to keep in the queue
 */
export declare const buffer_: <A, E>(self: Source<A, E>, bufferSize?: number | undefined, eager?: boolean) => Source<A, E>;
/**
 * Converts any type of stream into a pull based one.
 *
 * `bufferSize` configures the amount of items to keep in the queue
 */
export declare const buffer: (bufferSize?: number, eager?: boolean) => <A, E>(self: Source<A, E>) => Source<A, E>;
