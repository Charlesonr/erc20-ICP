import { Source } from "strict-callbag";
export declare const batchTime: (duration: number) => <A, E>(source: Source<A, E>) => Source<A, E>;
export declare const batchTime_: <A, E>(self: Source<A, E>, duration: number) => Source<A, E>;
