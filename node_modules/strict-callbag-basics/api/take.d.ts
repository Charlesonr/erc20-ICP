import { Source } from "strict-callbag";
export declare const take_: <A, E>(self: Source<A, E>, max: number) => Source<A, E>;
export declare const take: (max: number) => <A, E>(self: Source<A, E>) => Source<A, E>;
