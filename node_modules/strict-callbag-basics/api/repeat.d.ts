import { Source } from "strict-callbag";
export declare const repeat_: <A, E>(self: Source<A, E>, count: number) => Source<A, E>;
export declare const repeat: (count: number) => <A, E>(self: Source<A, E>) => Source<A, E>;
