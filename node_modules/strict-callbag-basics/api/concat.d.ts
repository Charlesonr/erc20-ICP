import { Source } from "strict-callbag";
export declare const concat_: <A, B, E, E1>(self: Source<A, E>, other: Source<B, E1>) => Source<A | B, E | E1>;
export declare const concat: <B, E1>(other: Source<B, E1>) => <A, E>(self: Source<A, E>) => Source<B | A, E1 | E>;
