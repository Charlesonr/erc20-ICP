export const error = (e) => (_, sink) => {
    let disposed = false;
    sink(0, (end) => {
        if (end !== 2)
            return;
        disposed = true;
    });
    if (disposed)
        return;
    sink(2, e);
};
