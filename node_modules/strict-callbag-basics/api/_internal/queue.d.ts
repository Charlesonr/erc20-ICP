import { NONE } from "../none.js";
export declare class Queue<A> {
    private capacity?;
    private elements;
    private index;
    size: number;
    constructor(capacity?: number | undefined);
    clear(): void;
    push(a: A): void;
    shift(): typeof NONE | A;
}
