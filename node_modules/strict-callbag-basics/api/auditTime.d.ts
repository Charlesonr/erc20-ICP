import { Source } from "strict-callbag";
export declare const auditTimeP_: <A, E>(self: Source<A, E>, ms: number) => Source<A, E>;
export declare const auditTimeP: (ms: number) => <A, E>(self: Source<A, E>) => Source<A, E>;
