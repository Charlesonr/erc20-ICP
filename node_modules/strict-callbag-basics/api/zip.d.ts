import { Source } from "strict-callbag";
export interface ZipOptions {
    latest?: boolean;
}
export declare const zip_: <A, B, E, E1>(self: Source<A, E>, other: Source<B, E1>, { latest }?: ZipOptions) => Source<readonly [A, B], E | E1>;
export declare const zip: <B, E1>(other: Source<B, E1>, opts?: ZipOptions) => <A, E>(self: Source<A, E>) => Source<readonly [A, B], E1 | E>;
