import { Source } from "strict-callbag";
export declare const batchCount_: <A, E>(self: Source<A, E>, batchSize: number) => Source<A[], E>;
export declare const batchCount: (batchSize: number) => <A, E>(self: Source<A, E>) => Source<A[], E>;
