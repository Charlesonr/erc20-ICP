export const drain = (self) => (_, sink) => {
    let talkback;
    return self(0, (signal, data) => {
        if (signal === 0) {
            talkback = data;
        }
        else if (signal === 1) {
            talkback(1);
            return;
        }
        sink(signal, data);
    });
};
