import { Source } from "strict-callbag";
export declare const batch: (seperator: Source<unknown, never>) => <A, E>(source: Source<A, E>) => Source<A, E>;
export declare const batch_: <A, E>(self: Source<A, E>, seperator: Source<unknown, never>) => Source<A, E>;
