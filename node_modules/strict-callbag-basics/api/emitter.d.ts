import { Sink } from "strict-callbag";
export interface Emitter<A, E> {
    data(this: void, a: A): void;
    error(this: void, e: E): void;
    end(this: void): void;
}
export declare const emitter: <A, E>(sink: Sink<A, E, never>) => Emitter<A, E>;
