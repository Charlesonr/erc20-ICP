import { Source } from "strict-callbag";
import { NONE } from "./none.js";
export declare const resource: <Acc, A, E, E1>(initial: Acc, project: (acc: Acc, index: number) => Source<readonly [typeof NONE | Acc, Source<A, E>], E1>, cleanup?: ((acc: Acc) => void) | undefined) => Source<A, E | E1>;
