// eslint-disable-next-line
export const noop = () => {
    let talkback;
    return (signal, data) => {
        if (signal === 0) {
            talkback = data;
            talkback(1);
        }
        else if (signal === 1) {
            talkback(1);
        }
    };
};
