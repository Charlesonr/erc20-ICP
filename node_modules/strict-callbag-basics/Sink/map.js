export const map_ = (self, f) => (signal, data) => {
    if (signal === 1) {
        self(1, f(data));
    }
    else {
        self(signal, data); // eslint-disable-line
    }
};
export const map = (f) => (self) => map_(self, f);
